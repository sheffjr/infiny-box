"""Infiny CLI — the terminal control block for Infiny OS.

A rich, modern chat/command interface that talks to the same Hermes gateway
(:8642) as the GUI — same brain, same memory, same skills. This is the
flagship Phase-1 client. A stdlib-only `--plain` fallback lives in `plain.py`
for recovery scenarios where third-party libs aren't available.
"""

__version__ = "1.0.0"
