name: infiny-render
version: 1.0.0
description: "Infiny page reader — renders JS pages with the Playwright/Chromium baked into the Box. Extract-only; pair with searxng for search."
author: Infiny
kind: backend
provides_web_providers:
  - infiny-render
